import { FunctionComponent } from "react";

/**
 * A component that shows a message saying that there is no data to show.
 */
export const NoData: FunctionComponent = () => {
  return <h2 className="h5 text-center my-5">No data to show.</h2>;
};
